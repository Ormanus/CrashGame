using UnityEngine;
using System.Collections;

public class cpuTrigger : MonoBehaviour
{

    Animator animator;
    Computer computer;

    // Use this for initialization
    void Start()
    {
        //gameObject.GetComponent<Animator>();

    }

    // Update is called once per frame
    void Update() // animator = getgameobject.getcompo..
    {

        /* if (computer.state == ComputerState.Running)
         {
             animator = gameObject.GetComponent<Animator>();
             
         }

         if (computer.state == ComputerState.Off)
         {
             animator = gameObject.GetComponent<Animator>();
             animator.SetBool("virtaa", false);
         }

         if (computer.state == ComputerState.Bluescreen)
         {
             animator = gameObject.GetComponent<Animator>();
             animator.SetBool("virtaa", true);
         } */
            }
}